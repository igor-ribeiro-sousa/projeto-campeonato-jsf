package teste;

public class Principal {
	public static void main(String[] args) {
//		// Nome da unidade de persistência definida no persistence.xml
//        String persistenceUnitName = "projetoJSF";
//
//        // Cria um EntityManagerFactory com base nas configurações definidas no persistence.xml
//        EntityManagerFactory entityManagerFactory = Persistence.createEntityManagerFactory(persistenceUnitName);
//
//        // Cria um EntityManager a partir do EntityManagerFactory
//        EntityManager entityManager = entityManagerFactory.createEntityManager();
//
//        // Agora você pode usar o entityManager para realizar operações de persistência
//        // Por exemplo, persistir um objeto em um banco de dados
//        Jogada meuObjeto = new Jogada();
//        entityManager.getTransaction().begin();
//        entityManager.persist(meuObjeto);
//        entityManager.getTransaction().commit();
//
//        // Não se esqueça de fechar o EntityManager e o EntityManagerFactory quando terminar
//        entityManager.close();
//        entityManagerFactory.close();
    }
}
